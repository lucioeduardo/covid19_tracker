import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:corona_data/app/modules/world/world_controller.dart';
import 'package:corona_data/app/modules/world/world_module.dart';

void main() {
  initModule(WorldModule());
  // WorldController world;
  //
  setUp(() {
    //     world = WorldModule.to.get<WorldController>();
  });

  group('WorldController Test', () {
    //   test("First Test", () {
    //     expect(world, isInstanceOf<WorldController>());
    //   });

    //   test("Set Value", () {
    //     expect(world.value, equals(0));
    //     world.increment();
    //     expect(world.value, equals(1));
    //   });
  });
}
