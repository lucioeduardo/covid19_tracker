import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:corona_data/app/modules/world/world_page.dart';

main() {
  testWidgets('WorldPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(WorldPage(title: 'World')));
    //  final titleFinder = find.text('World');
    //  expect(titleFinder, findsOneWidget);
  });
}
