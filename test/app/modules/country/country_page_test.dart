import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:corona_data/app/modules/country/country_page.dart';

main() {
  testWidgets('CountryPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CountryPage(title: 'Country')));
    //  final titleFinder = find.text('Country');
    //  expect(titleFinder, findsOneWidget);
  });
}
